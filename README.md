# LAAC-LSCP-documentation


## Building the documentation
The documentation is generated using the [zensical](https://zensical.org/) static-site generator.

The content is available in the `docs/` folder and the zensical configuration file is the `zensical.toml` one.

```bash
uv run zensical serve
```
